/**
 * Simulator version (spec §101, SIM-019). Recorded on every attempt. Bumped when simulated
 * behavior changes so historical evidence stays bound to the engine that graded it.
 * The engine itself is Phase 10; nothing simulates yet.
 */
export const SIMULATOR_VERSION = '0.0.0';
