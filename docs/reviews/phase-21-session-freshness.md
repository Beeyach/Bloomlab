# Phase 21 — clean restart and deployed-session freshness

Execution of the complete [session-freshness handoff](../handoffs/phase-21-real-device-session-freshness.md) on draft/open [PR #23](https://github.com/Beeyach/Bloomlab/pull/23). Starting commit: `155aaf5` (handoff atop `219e2efc326b93b48f7930398c8e45f98d2e73d5`). No scenario content, engine, interpretation, max-turn, grading, speaker-attribution, provider, privacy or budget contract is relaxed. No old migration is edited. Preview only; production remains gated and its deployment must be skipped.

## Findings and changes

The physical iPhone Northwind session resumed earlier off-path work, then reached turn 7. That is not a fresh routing failure. The device also showed none of the verified `219e2ef` loading UI; an older loaded PWA bundle is the likely cause, not a proven new loading-component failure. ExerciseRunner intentionally resumes unfinished work, and the old registration relied on automatic service-worker activation/reload without a visible freshness check.

**Start fresh call** opens a confirmation that names all raw recordings, including retained audio. Cancel leaves the attempt intact. Confirm uses the existing owner-authenticated inventory/deletion paths, including unacknowledged R2-first audio, before removing local Blobs. Partial failure preserves the old attempt and remaining audio with an explicit retry message; already completed deletions are not represented as reversible. After cleanup, one queued IndexedDB transaction creates a new ID and empty attempt. Start call then opens at turn 0. No empty-workspace gap, abandoned result, rubric submission, sync outbox or mastery evidence is created. Private server call/transcript/turn metadata, deletion tombstones, usage claims and generated client-voice cache remain; restart is not account erasure. See [operations](../operations/call-room.md).

The PWA uses a waiting worker and explicit **Reload to update**. Startup/visible return/focus/reconnect/minute checks combine the registration state with an uncached Worker build identity. A waiting worker, new controller or build mismatch announces **Update available**. Discovery and activation never authorize reload by themselves. Recording permission/capture, Blob and metadata checkpoints, upload/STT, transcript review, pending confirmation, audio operations and final feedback hold reload. Finishing a checkpoint enables the button; it still requires a click. Another tab's activation cannot discard live work in this page. Registration/check failures retain a visible, actionable retry state. Reduced motion does not hide status.

CI embeds the PR source SHA (not a synthetic merge SHA) in browser and Worker. The actual loaded frame has `data-build-id`; `/system` shows both identities, and uncached `/api/health` supplies the Worker identity. These fields contain no provider configuration or credentials. Offline shell/curriculum caching and `/api/*` NetworkOnly remain enabled. Already-loaded historical JavaScript cannot acquire these changes retroactively; first adoption follows the normal service-worker lifecycle, without storage clearing or unsafe forced reload. Subsequent version differences are visible.

## Verification

- Focused restart/PWA/Call Room/Worker routing: **54 regressions passed in the full suite**. Restart covers untouched turn 0, confirmed turns, unsent local Blob, uploaded/unretained audio, retained consent, partial cleanup/retry, authentication failure, new ID, no old resume and no evidence. The Call Room capture regression holds local storage after Stop and verifies reload remains blocked until the Blob and checkpoint finish.
- Real two-build PWA probe: `local-pwa-a` → `local-pwa-b`, **six scenarios passed** (idle, recording, pending Blob save, transcript review, pending turn, final feedback). Five unsafe scenarios force real worker activation as another tab could; the old page remains loaded until work settles and an explicit reload is chosen. Saved audio survives capture/save scenarios, the active attempt survives review/turn scenarios, and completed feedback survives submission. Five-width accessible/reduced-motion notification captures accompany the report.
- `review:call` uses native Chromium MediaRecorder and IndexedDB with a virtual microphone and explicit HTTP fixtures. It verifies the actual loaded browser/Worker identities, five widths (1440/1024/768/390/320), touch, keyboard/focus, correction, same-Blob retry, retained audio, loading/error/retry/duplicate prevention, advanced-aid exclusion and restart confirmation/cancel/failure/retry. A new ID survives reload at opening and the four authored confirmed phrases reach `done`. The real Worker integration test independently resolves `opening → problem → process → agreed → done` with `move: null`, four rule-interpreted turns and no classifier. The recovery ceiling stays 12.
- Full pinned Node 22.22.1 CI passed: **1,810 tests across 111 files**, typecheck, lint (only the existing ExerciseRunner dependency warning), formatting, control-doc/content/voice validation and build/provider-boundary scan. Final deployed evidence is recorded in the final PR checks/description and [sanitized evidence](phase-21-session-freshness-evidence.json). The final source head must equal both deployed build identities. Post-deployment call, offline and saved-voice probes remain separately scoped; no virtual-microphone result is physical acceptance.

Reproduce the PWA probe by building Preview twice with `BLOOMLAB_BUILD_ID=local-pwa-a` and `local-pwa-b`, copying each `apps/web/dist/client` directory, then running `node scripts/review/pwa-update-probe.mjs` with `PWA_OLD`/`PWA_NEW` pointing to those copies. Use the pinned Node 22 and documented Chromium virtual-microphone flags from the Call Room probe. The built assets and screenshots remain ignored review artifacts, not learner data in source control.

## Human evidence and limits

Preserved observations: iPhone 14 Pro Max Safari microphone capture and offline transcription retry from the same saved recording passed. Retained audio deletion disabled replay, but the user did not hear retained replay audio on iPhone. Mac Chrome completed proposal mechanics, transcript correction/history and TTS text fallback; feedback recovered on the original saved four-turn attempt without recording again. Mac Safari Private Blob persistence failed. Repeated iPhone microphone permissions, perceived slowness and scripted roleplay remain limitations. The resumed turn-7/older-bundle session verifies neither fresh routing nor the new loading UI.

No CALL-005 or broader human status is promoted by these probes. CALL-001/CALL-003/CALL-004/VOI-003 retain their supported evidence; the seven broader human rows stay IMPLEMENTED_UNVERIFIED. No human credentials, recordings, transcripts or feedback prose were inspected. This follow-up ends before asking for another physical-device run. PR #23 remains draft/open and unmerged for independent audit.
