# Release stamp of the curriculum (spec §101, CNT-007). Bump content_version whenever any file
# under content/ changes, then run `npm run content:lock`. Git = what Bloomlab teaches.
content_version: '2026.09.07'
schema_version: 1
notes: >-
  Phase 12 — the Workflow Settings registry record, re-verified wait, appointment and reply
  records (cancellation and reschedule behaviour, filters), structured wait and If/Else configs,
  and grading purposes on the authored SMS steps.
