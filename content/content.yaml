# Release stamp of the curriculum (spec §101, CNT-007). Bump content_version whenever any file
# under content/ changes, then run `npm run content:lock`. Git = what Bloomlab teaches.
content_version: '2026.09.12'
schema_version: 1
notes: >-
  Phase 15 — Glowhaus's three weeks as 269 scheduled events (SC-glowhaus-reporting), nine incident
  scenarios behind the Incident Room, the FUNNEL AUTOPSY and the graded bottleneck exercise, and
  the outbound webhook record re-checked for headers and authorization. The loop incident
  now holds its contact from the first instant, so its Reproduce it action is valid whenever a
  learner fires it (D-150).
