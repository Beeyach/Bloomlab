# content/

Curriculum lives here as Git-tracked YAML + Markdown/MDX (spec §98, `CONTENT_ARCHITECTURE.md`). The tree — skills, ghl-features, campaigns, learning-units, exercises, scenarios, clients, rubrics, projects, portfolio, glossary — is created with the content engine in Phase 5. Nothing is compiled from this folder yet; `CONTENT_VERSION` is `null` until then.
