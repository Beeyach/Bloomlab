# Verified 2026-09-02 against "Workflow Action - Webhook (Outbound)": POST by default, custom
# key-value data with merge fields, no files, payload not shown in execution logs.
# The separate Custom Webhook premium action is registered independently; this record is only the
# standard outbound Webhook action.
id: GHL-WF-WEBHOOK
official_name: Webhook
area: Workflows
feature_type: action
implementation_type: native_ghl
status: current
simulation_fidelity: B
last_verified: 2026-09-06
source_url: https://help.gohighlevel.com/support/solutions/articles/155000003299-workflow-action-webhook-outbound-
known_limitations:
  - Cannot send files or images.
  - The payload only carries data related to the triggering event.
  - Execution logs confirm the call but do not show the payload.
  - The standard Webhook action is represented for comparison, but Bloomlab's runnable HTTP-method, header and authentication exercise uses the separately registered Custom Webhook action.
skills:
  - SK-CONNECT-webhook-payloads
supported_configs:
  config_fields:
    - name: url
      type: string
      required: true
    - name: custom_data
      type: json
      required: false
      description: Key-value pairs; values may use merge fields.
approximation_note: >-
  Registry-backed comparison only. The standard action is not executed in Bloomlab; the separate
  Custom Webhook record owns the deterministic request-contract simulation.
verification_note: >-
  Name, default method, custom data and the first three limitations confirmed on the help-center
  article, 2026-09-02. Re-checked 2026-09-04 through search summaries of the official article
  (the build environment's proxy blocks the help centre): the outbound webhook posts a
  context-dependent payload to a URL with optional Custom Data key/value rows. The distinct
  Custom Webhook contract is no longer conflated with this record.
